/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var twoSum = function(nums, target) {
    var numsLength = nums.length;
    for (var addedIndex = 0; nums[addedIndex++]; addedIndex++){
        for (var adderIndex = 0; adderIndex != addedIndex && nums[adderIndex++]; adder++) {

        }
        num[multiplicandIndex]
    }
};