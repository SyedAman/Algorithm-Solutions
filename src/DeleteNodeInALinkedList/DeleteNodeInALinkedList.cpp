#include <cstddef>

// Definition for singly-linked list.
struct ListNode
{
    int data;
    ListNode *next;
    ListNode(int x) : data(x), next(NULL) {}
};

// Solution
class Solution
{
  public:
    void deleteNode(ListNode *node)
    {
    }
};

// Test solution
int main()
{
    return 0;
};